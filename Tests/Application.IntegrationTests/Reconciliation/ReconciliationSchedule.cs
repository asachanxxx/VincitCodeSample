using System;

namespace Tests.Application.IntegrationTests.Reconciliation
{
    public class ReconciliationSchedule
    {
        //[Test]
        //public async Task ShouldReturnUser()
        //{
        //    string email = RunAsUser.WorkEmail;

        //    var cmd = new CreateReconciliationScheduleRequestCommand();
        //    cmd.AccountRegisterId = 1;
        //    cmd.Frequency = ReconciliationFrequencySEnum.FromValue(1);
        //    var result = await SendAsync(cmd);
        //    result.Should().BeGreaterThan(0);
        //}
    }
}
